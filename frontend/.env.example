# Base URL of the management API. In dev, Vite's proxy forwards /api/*
# to http://localhost:8787, so leave this empty. In production, set to
# the deployed API worker origin (e.g. https://api.normieagent.com).
VITE_API_BASE=
